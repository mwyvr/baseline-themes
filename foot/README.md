# baseline for foot

```sh
cp baseline-*.ini ~/.config/foot/
```

Include one theme from `foot.ini`:

```ini
include=~/.config/foot/baseline-dark.ini
```

foot reports light/dark to applications (mode 2031); theme selection itself
is static.
